# 期权监控 Ubuntu 服务器部署指南

本文适用于 Ubuntu 22.04 或 24.04。以下命令假设服务器上的运行账号是 `optionmonitor`，项目目录为 `/opt/option-monitor`。请使用有 `sudo` 权限的普通管理员账号执行。

> 不要从 Windows 复制 `.env`、`state`、SQLite 文件或 `openvlab-browser-profile`。服务器必须重新填写密钥，并在服务器的专用浏览器配置中重新登录 OpenVLab。

## 1. 安装系统依赖并创建运行账号

```bash
sudo apt update
sudo apt install -y git python3 python3-venv fonts-noto-cjk
sudo useradd --system --create-home --home-dir /var/lib/optionmonitor --shell /usr/sbin/nologin optionmonitor
sudo install -d -o optionmonitor -g optionmonitor /opt/option-monitor
```

如果服务器已经有 `optionmonitor` 账号，`useradd` 会提示已存在，可跳过该条命令。

## 2. 拉取代码

公开仓库可直接拉取：

```bash
sudo -u optionmonitor -H git clone https://github.com/xunme1/option_track.git /opt/option-monitor
```

若将 GitHub 仓库改为私有仓库，请为 `optionmonitor` 配置只读 Deploy Key，或使用 GitHub App/短期 Token；不要把个人访问令牌写入仓库、`systemd` 服务文件或 Shell 历史。

## 3. 创建 Python 环境并安装浏览器

```bash
sudo -u optionmonitor -H python3 -m venv /opt/option-monitor/.venv
sudo -u optionmonitor -H /opt/option-monitor/.venv/bin/pip install --upgrade pip
sudo -u optionmonitor -H /opt/option-monitor/.venv/bin/pip install -r /opt/option-monitor/requirements.txt
sudo /opt/option-monitor/.venv/bin/playwright install-deps chromium
sudo -u optionmonitor -H /opt/option-monitor/.venv/bin/playwright install chromium
```

前两类命令分别安装 Chromium 的操作系统依赖和当前运行账号可使用的 Chromium。两者都需要完成，OpenVLab 无头截图才可运行。

## 4. 配置密钥与业务参数

```bash
sudo -u optionmonitor -H cp /opt/option-monitor/.env.example /opt/option-monitor/.env
sudo chmod 600 /opt/option-monitor/.env
sudo chown optionmonitor:optionmonitor /opt/option-monitor/.env
sudoedit /opt/option-monitor/.env
```

至少填写以下项：

```dotenv
ORANGE_API_TOKEN=请填写
RQDATA_API_KEY=请填写
DINGTALK_WEBHOOK=请填写
DINGTALK_SECRET=请填写
ALIYUN_OSS_ACCESS_KEY_ID=请填写
ALIYUN_OSS_ACCESS_KEY_SECRET=请填写
ALIYUN_OSS_REGION=请填写，例如 cn-hangzhou
ALIYUN_OSS_BUCKET=请填写
ALIYUN_OSS_ENDPOINT=请填写，例如 oss-cn-hangzhou.aliyuncs.com
ALIYUN_OSS_PREFIX=option-monitor/charts
```

`ALIYUN_OSS_PREFIX` 是 Bucket 内保存图表对象的目录前缀，不是本机路径。若使用 Playwright 自带 Chromium，保持 `OPENVLAB_BROWSER_CHANNEL` 为空；只有使用系统 Chrome 时才填 `chrome`。

## 5. 验证核心监控

先进行不上传 OSS、不发钉钉的验证：

```bash
sudo -u optionmonitor -H /opt/option-monitor/.venv/bin/python /opt/option-monitor/scripts/run_options_monitor.py --root /opt/option-monitor --dry-run
```

命令成功后会输出 `MANIFEST_PATH=...`。检查对应 `state/outbox/<run_id>/manifest.json` 的 `status`、覆盖率和错误信息。

需要验证完整链路时，在交易时段运行以下命令；它会采集全部品种、上传 OSS，并向钉钉群投递测试消息：

```bash
sudo -u optionmonitor -H /opt/option-monitor/.venv/bin/python /opt/option-monitor/scripts/run_and_deliver_options_monitor.py --root /opt/option-monitor --force-anomaly-report --force-all-products --require-full-coverage
```

## 6. 初始化 OpenVLab 登录态

OpenVLab 必须在服务器上重新登录一次。使用服务器控制台桌面、VNC 或 X11 转发打开可见浏览器后，以运行账号执行：

```bash
sudo -u optionmonitor -H /opt/option-monitor/.venv/bin/python /opt/option-monitor/scripts/open_openvlab_login.py --root /opt/option-monitor
```

在弹出的 Chromium 内登录 OpenVLab，并确认可看到期权排名表；随后关闭浏览器窗口。成功后会输出 `OpenVLab login initialized`。登录态仅保存在：

```text
/opt/option-monitor/state/openvlab-browser-profile
```

验证无头截图：

```bash
sudo -u optionmonitor -H /opt/option-monitor/.venv/bin/python /opt/option-monitor/scripts/capture_openvlab_ranking.py --root /opt/option-monitor --output /opt/option-monitor/state/openvlab-ranking-test.png
```

## 7. 启用定时任务

部署文件已在仓库内。默认配置使用 `optionmonitor` 账号。`option-monitor.timer` 在工作日北京时间 10:15、14:30 静默采集（生成异常长图、上传 OSS、产出钉钉文案，但**不投递**；群推送由龙虾 OpenClaw 连接器的定时任务完成）；`option-monitor-close.timer` 在工作日 15:00 收盘时额外静默采集一次，作为 RR25 日度收盘基线。两个 service 均执行 `deploy/option-monitor-capture.sh`。

```bash
sudo cp /opt/option-monitor/deploy/systemd/option-monitor.service /etc/systemd/system/
sudo cp /opt/option-monitor/deploy/systemd/option-monitor.timer /etc/systemd/system/
sudo cp /opt/option-monitor/deploy/systemd/option-monitor-close.service /etc/systemd/system/
sudo cp /opt/option-monitor/deploy/systemd/option-monitor-close.timer /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable --now option-monitor.timer option-monitor-close.timer
systemctl list-timers option-monitor.timer option-monitor-close.timer
```

立即执行一次服务用于排错：

```bash
sudo systemctl start option-monitor.service
sudo systemctl status option-monitor.service --no-pager
sudo journalctl -u option-monitor.service -n 100 --no-pager
```

## 供龙虾读取推送文字

如由服务器上的龙虾负责编辑或发送文字，可在监控生成 ready 清单后调用以下脚本。脚本只把 DingTalk Markdown 正文写到标准输出，不会采集、上传或发送：

```bash
sudo -u optionmonitor -H /opt/option-monitor/.venv/bin/python /opt/option-monitor/scripts/output_monitor_message_text.py --root /opt/option-monitor --run-id <run_id>
```

龙虾接管某个清单的发送后，不要再让 `run_and_deliver_options_monitor.py` 投递同一清单，否则会出现两个发送方。生产环境可选择标准 systemd 钉钉投递，或改由龙虾发送，两者二选一。

## 供龙虾即时生成单品种期权图

针对“甲醇的期权情况”“黄金期权怎么样”这类即时问题，龙虾应调用 `render_instant_option_chart.py`。该命令每次实时访问期货报价与 Orange Hitick 期权接口，不使用之前的采集结果；不会创建监控运行记录、上传 OSS 或向钉钉发送消息。

调用参数规范如下：

| 参数 | 是否必填 | 规范 |
| --- | --- | --- |
| `--product` | 是 | `PRODUCTS` 中的中文品种名或代码，例如 `甲醇` / `MA`。不要传入具体合约（如 `MA2609`）。 |
| `--output` | 是 | 由龙虾为本次请求生成的、唯一的 `.png` 输出路径。建议使用绝对路径；脚本会创建父目录。禁止复用旧图片路径。 |
| `--contract` | 否 | 指定期货合约，例如郑商所 `MA609`、中金所 `IF2609`。不传时解析当前主力；传入后严格采集指定合约，不可用即失败，绝不回退。 |
| `--root` | 否 | 项目根目录，部署示例为 `/opt/option-monitor`。 |
| `--trading-day` | 否 | `YYYYMMDD`，默认北京时间当天；仅在排障或指定交易日时使用。 |

示例：

```bash
sudo -u optionmonitor -H /opt/option-monitor/.venv/bin/python /opt/option-monitor/scripts/render_instant_option_chart.py \
  --root /opt/option-monitor \
  --product 甲醇 \
  --output /var/tmp/lobster/option-ma-20260826-001.png
```

若用户明确询问一个期货合约，龙虾应同时传入与品种匹配的 `--contract`：

```bash
sudo -u optionmonitor -H /opt/option-monitor/.venv/bin/python /opt/option-monitor/scripts/render_instant_option_chart.py \
  --root /opt/option-monitor \
  --product 甲醇 \
  --contract MA609 \
  --output /var/tmp/lobster/option-ma609-20260826-001.png
```

成功（退出码 `0`）后，脚本只向标准输出写入：

```text
IMAGE_PATH=/var/tmp/lobster/option-ma-20260826-001.png
PRODUCT_CODE=MA
UNDERLYING=MAxxx
```

龙虾必须仅在退出码为 `0` 且 `IMAGE_PATH` 指向存在的 PNG 时发送该图；失败时不要回退到任何旧图。图中四格分别为：期货最新价与 `(最新价 - 开盘价) / 开盘价` 的日内涨跌、ATM IV、Call/Put 较昨持仓变化、RR25 偏度。ΔRR25 为当前值减去本地 `state/option_monitor.sqlite3` 中上一交易日收盘基线（15:00 收盘采集写入，14:30 定时任务兜底）；没有该基线时显示 `--`，绝不以两次临时盘中采集替代。ΔIV 与 RR25 十日排名不计算或展示。

此命令至少需要 `.env` 的 `ORANGE_API_TOKEN`；`RQDATA_API_KEY` 可选，未设置或不可用时会自动回退东方财富报价。中文字体由 `fonts-noto-cjk` 提供；若系统路径不同，在 `.env` 设置 `OPTION_MONITOR_FONT_PATH` 为有效 `.ttc`/`.ttf` 文件。

## 8. 更新代码

建议在非运行时段更新：

```bash
sudo systemctl stop option-monitor.timer
sudo -u optionmonitor -H git -C /opt/option-monitor pull --ff-only
sudo -u optionmonitor -H /opt/option-monitor/.venv/bin/pip install -r /opt/option-monitor/requirements.txt
sudo -u optionmonitor -H /opt/option-monitor/.venv/bin/playwright install chromium
sudo systemctl start option-monitor.timer
```

`.env`、`state` 和 OpenVLab 登录态均被 Git 忽略，拉取更新不会覆盖它们。若更新了 `deploy/systemd` 下的文件，重新复制服务文件并执行 `sudo systemctl daemon-reload`。

## 常见问题

| 现象 | 处理方式 |
| --- | --- |
| `OpenVLab login is not initialized` | 按第 6 节在服务器上重新登录。 |
| `OpenVLab browser is unavailable` | 重新执行第 3 节的两个 Playwright 安装命令，并查看 `journalctl`。 |
| 环境变量缺失 | 检查 `/opt/option-monitor/.env` 是否存在、权限是否为 `600`，以及键名是否正确。 |
| 图表未到钉钉 | 查看服务日志和 `state/outbox` 中的 `manifest.json`；依次核对 OSS、钉钉 Webhook/签名、OpenVLab 登录态。 |
| 时间不对 | 执行 `timedatectl`；`systemd` 定时器已显式使用 `Asia/Shanghai`。 |
